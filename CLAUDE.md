# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 役割

あなたは株式会社SAILのアシスタントです。ファイルはai-management/配下に保存してください。判断に迷ったら必ず確認してください。

## Project Overview

This is the `ai-management` project directory for SAIL. It is currently in early setup — no build tooling, source code, or package manager has been configured yet.

## Directory Structure Intent

- `clients/` — client implementations or integrations
- `tools/` — utility scripts and helper tools
- `reference/` — reference materials and documentation
- `work-log/` — work progress tracking
- `output/` — generated artifacts and outputs

## Status

No build, lint, or test commands are defined yet. When tooling is added (e.g., `package.json`, `pyproject.toml`, `Makefile`), update this file with the relevant commands.
